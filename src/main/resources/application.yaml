spring:
  datasource:
    # ?????????
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/caizhitong?connectTimeout=30000&remarks=true&useInformationSchema=true&tinyInt1isBit=true
    username: root
    password: 123456

    # Druid ???????
    druid:
      keep-alive: true #????
      keep-alive-between-time-millis: 60000
      initial-size: 5          # ????????? CPU??/2?
      min-idle: 5              # ????????? CPU??*1.5?
      max-active: 20           # ????????????????
      max-wait: 60000          # ??????????????
      time-between-eviction-runs-millis: 60000   # ??????????
      min-evictable-idle-time-millis: 300000     # ????????
      validation-query: SELECT 1  # ???????SQL
      test-while-idle: true      # ???????????
      test-on-borrow: false      # ?????????????
      test-on-return: false      # ???????
      max-pool-prepared-statement-per-connection-size: 20
sa-token:
  timeout: 43200
  token-name: satoken
  active-timeout: 1800
  is-concurrent: true #多地登录
  is-share: false #不允许多个账号共享token
  token-style: uuid
  is-log: true
mybatis-plus:
  global-config:
    db-config:
      update-strategy: not_null
